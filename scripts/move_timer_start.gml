//this function sets all the timers for running
//input 0 = basic move timer (how many game steps to pause for)
//input 1 = a multiplier to apply to the move timer
// TODO: use an acceleration value instead of multipler?
// TODO: give this an input, call it move_direction

move_timer = true;
move_timer_count = 0;
move_timer_total_init = argument0;
