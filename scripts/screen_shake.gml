// sets the game's shake values to whatever arugments are passed to it
// in theory, can be called by any instance?
oScreen.shaking = true;
oScreen.shake_count = argument0;
oScreen.shake_intensity = argument1;
